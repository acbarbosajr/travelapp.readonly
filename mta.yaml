_schema-version: "3.2"
ID: travelappreadonly
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travelappreadonly-destination-content
  type: com.sap.application.content
  requires:
  - name: travelappreadonly-destination-service
    parameters:
      content-target: true
  - name: travelappreadonly-repo-host
    parameters:
      service-key:
        name: travelappreadonly-repo-host-key
  - name: travelappreadonly-uaa
    parameters:
      service-key:
        name: travelappreadonly-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travelappreadonly_html_repo_host
          ServiceInstanceName: travelappreadonly-html5-service
          ServiceKeyName: travelappreadonly-repo-host-key
          sap.cloud.service: travelappreadonly
        - Authentication: OAuth2UserTokenExchange
          Name: travelappreadonly_uaa
          ServiceInstanceName: travelappreadonly-xsuaa-service
          ServiceKeyName: travelappreadonly-uaa-key
          sap.cloud.service: travelappreadonly
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: travelappreadonly-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travelappreadonly-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travelappreadonly.zip
      name: travelappreadonly
      target-path: resources/
- name: travelappreadonly
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travelappreadonly-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travelappreadonly-destination-service
    service-plan: lite
- name: travelappreadonly-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travelappreadonly-xsuaa-service
    service-plan: application
- name: travelappreadonly-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travelappreadonly-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
